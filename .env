# Local .env moved to secrets/.env for safety.
# Do NOT store real secrets here. See `secrets/.env` (ignored by Git).
# Example placeholders (auskommentiert für Dev-Modus):
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=you@example.com
# SMTP_PASS=REPLACE_ME

# In Dev-Modus ohne SMTP werden Mails in server/mail-dev.log gespeichert
TO_EMAIL=info@goldschmiedeatelier-krauss.de
